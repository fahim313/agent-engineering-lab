[project]
name = "agent-engineering-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Fahim sahriar", email = "fahimsahriar2002@gmail.com" }
]
requires-python = ">=3.13.3"
dependencies = [
    "langchain>=1.4.2",
    "langchain-groq>=1.1.3",
    "langgraph>=1.2.12",
    "python-dotenv>=1.2.3",
    "tavily-python>=0.8.4",
]

[project.scripts]
agent-engineering-lab = "agent_engineering_lab:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
